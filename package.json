{
  "name": "checkmate",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start --prefix to-do-backend\" \"npm run start --prefix to-do\"",
    "install-all": "npm install --prefix to-do-backend && npm install --prefix to-do",
    "build": "npm run build --prefix to-do",
    "dev": "concurrently \"npm run dev --prefix to-do-backend\" \"npm start --prefix to-do\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@fortawesome/free-solid-svg-icons": "^6.5.2",
    "@fortawesome/react-fontawesome": "^0.2.2",
    "axios": "^1.7.2",
    "react-modal": "^3.16.1"
  }
}
